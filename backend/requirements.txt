# CareCircle Backend Requirements
# ML runs via HuggingFace Inference API (free) - no local GPU/RAM hit
# XGBoost + CatBoost are tiny (<5MB each) and run on CPU fine

# Core API
fastapi==0.109.2
uvicorn[standard]==0.27.1
python-multipart==0.0.9
pydantic>=2.8.0
pydantic-settings>=2.2.1
python-dotenv==1.0.1
httpx==0.25.0
aiofiles==23.2.1

# Database
asyncpg>=0.29.0

# AI - Gemini (cloud, zero local RAM)
google-generativeai==0.4.1

# ML - lightweight CPU only, no GPU needed
scikit-learn>=1.5.0
xgboost>=2.1.0
catboost>=1.2.7
pandas>=2.2.0
numpy>=1.26.4
joblib>=1.3.2

# HuggingFace Inference API (FREE cloud - no local model download)
huggingface-hub==0.21.0

# OCR
pytesseract==0.3.10
pillow>=10.2.0

# Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
